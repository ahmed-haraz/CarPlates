<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:CarPlates.Mobile.ViewModels"
             xmlns:dtos="clr-namespace:CarPlates.Application.Common.DTOs;assembly=CarPlates.Application"
             xmlns:loc="clr-namespace:CarPlates.Mobile.Localization"
             x:Class="CarPlates.Mobile.Views.Vehicle.VehicleDetailsPage"
             x:DataType="vm:VehicleDetailsViewModel"
             Title="{Binding Title}">

    <ScrollView>
        <VerticalStackLayout Padding="16" Spacing="16">
            <!-- Plate Card -->
            <Border Style="{StaticResource ElevatedCard}" Padding="24">
                <VerticalStackLayout HorizontalOptions="Center" Spacing="8">
                    <Label Text="🚗" FontSize="48" HorizontalOptions="Center"/>
                    <Label Text="{Binding VehicleDetails.PlateNumber}" 
                           Style="{StaticResource Headline}"
                           HorizontalOptions="Center"
                           TextColor="{StaticResource Primary}"/>
                    <Label Text="{Binding VehicleDetails.AccessStatus}" 
                           TextColor="{StaticResource Success}"
                           FontAttributes="Bold"
                           HorizontalOptions="Center"/>
                </VerticalStackLayout>
            </Border>

            <!-- Vehicle Info Grid -->
            <Border Style="{StaticResource Card}">
                <Grid RowDefinitions="Auto,Auto,Auto,Auto,Auto" ColumnDefinitions="Auto,*" RowSpacing="12" ColumnSpacing="16">
                    <Label Grid.Row="0" Grid.Column="0" Text="{loc:Translate Key=BrandColon}" Style="{StaticResource Body}" FontAttributes="Bold"/>
                    <Label Grid.Row="0" Grid.Column="1" Text="{Binding VehicleDetails.Brand}" Style="{StaticResource Body}"/>

                    <Label Grid.Row="1" Grid.Column="0" Text="{loc:Translate Key=ModelColon}" Style="{StaticResource Body}" FontAttributes="Bold"/>
                    <Label Grid.Row="1" Grid.Column="1" Text="{Binding VehicleDetails.Model}" Style="{StaticResource Body}"/>

                    <Label Grid.Row="2" Grid.Column="0" Text="{loc:Translate Key=ColorColon}" Style="{StaticResource Body}" FontAttributes="Bold"/>
                    <Label Grid.Row="2" Grid.Column="1" Text="{Binding VehicleDetails.Color}" Style="{StaticResource Body}"/>

                    <Label Grid.Row="3" Grid.Column="0" Text="{loc:Translate Key=OwnerColon}" Style="{StaticResource Body}" FontAttributes="Bold"/>
                    <Label Grid.Row="3" Grid.Column="1" Text="{Binding VehicleDetails.OwnerName}" Style="{StaticResource Body}"/>

                    <Label Grid.Row="4" Grid.Column="0" Text="{loc:Translate Key=TotalScansColon}" Style="{StaticResource Body}" FontAttributes="Bold"/>
                    <Label Grid.Row="4" Grid.Column="1" Text="{Binding VehicleDetails.TotalScans}" Style="{StaticResource Body}"/>
                </Grid>
            </Border>

            <!-- Scan History -->
            <Label Text="{loc:Translate Key=ScanHistory}" Style="{StaticResource Title}" Margin="0,8,0,0"/>
            <CollectionView ItemsSource="{Binding ScanHistory}"
                          EmptyView="{loc:Translate Key=NoScanHistoryAvailable}">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="dtos:ScanRecordDto">
                        <Border Style="{StaticResource Card}" Margin="0,4">
                            <Grid ColumnDefinitions="*,Auto">
                                <Label Grid.Column="0" 
                                       Text="{Binding ScanTime, StringFormat='{0:MMM dd, yyyy HH:mm}'}" 
                                       Style="{StaticResource Body}"/>
                                <Label Grid.Column="1" 
                                       Text="{Binding Confidence, StringFormat='{0:P0}'}" 
                                       Style="{StaticResource Caption}"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <!-- Action Buttons -->
            <Grid ColumnDefinitions="*,*" ColumnSpacing="12">
                <Button Grid.Column="0"
                        Text="{loc:Translate Key=ScanAgainButton}"
                        Style="{StaticResource PrimaryButton}"
                        Command="{Binding ScanAgainCommand}"/>
                <Button Grid.Column="1"
                        Text="{loc:Translate Key=ShareButton}"
                        Style="{StaticResource SecondaryButton}"
                        Command="{Binding ShareCommand}"/>
            </Grid>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
